option_settings:
    aws:elasticbeanstalk:container:nodejs:
        NodeCommand: "npm run install-all && npm run web-build && npm run api-build && npm start"
    aws:elasticbeanstalk:application:environment:
        NODE_ENV: PRODUCTION