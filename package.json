{
  "name": "richmun-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm i && (npm run build-web) && npm run build-linux && node dist/index.js",
    "serve": "npm run build-linux && node dist/index.js",
    "build-windows": "rimraf dist && npm run build-ts",
    "build-linux": "rm -rf dist && npm run build-ts",
    "build-web": "cd web && npm i && npm run build",
    "build-ts": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/body-parser": "^1.19.0",
    "@types/express": "^4.17.6",
    "@types/node": "^14.0.6",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "rimraf": "^3.0.2",
    "ts-node": "^8.10.1",
    "typescript": "^3.9.3"
  }
}
